using UnityEngine;

public class teste : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created teste 1
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
